#!/bin/bash

if [ "x$HELP" != "x" ]
then
    echo "git redmine self-upgrade-devel"
    exit 1
fi

set -e

echo "Self-Upgrading git redmine to devel ..."

cd /tmp
rm -rf redmine-suite
git clone git://gitorious.celogeek.com/git/redmine-suite.git
cd redmine-suite
git checkout devel
./install.sh
