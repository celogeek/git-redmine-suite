#!/bin/bash

if [ "x$HELP" != "x" ]
then
    echo "git redmine task todo"
    exit 1
fi

set -e

source /usr/local/share/Git-Redmine-Suite/helpers/redmine-checkconf
source /usr/local/share/Git-Redmine-Suite/helpers/redmine-tasks

echo "The status of your local tasks : "
for TASK in $(redmine_get_all_local_tasks)
do
    echo -n "    "
    /usr/local/share/Git-Redmine-Suite/helpers/redmine-get-title --auth_key="$REDMINE_AUTHKEY" --server_url="$REDMINE_URL" --id="$TASK" --with-status
done
echo ""

